@model ProductVM.Models.ProductIndexViewModel
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<ul>
    @foreach(var product in Model.Products) {
        <li>
            @product.Name
        </li>
    }
</ul>

