@helper Currency(decimal amount) {
    if (amount < 0) {
        <b style="color:red">@amount.ToString("c")</b>
    } else {
        @amount.ToString("c")
    }
}

@helper TwoDigits(double number) {
    @String.Format("{0:0.00}",number)
}
